/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "thumbs.dreamstime.com", "media.istockphoto.com", "fakestoreapi.com"
        ]
    }
};

export default nextConfig;
